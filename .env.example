# Path to your Chrome user data directory (to reuse a logged-in session)
# Example on Linux: /home/you/.config/google-chrome
CHROME_PROFILE_PATH=

# Optional: headless browser (true/false)
HEADLESS=true

# Optional: if not using profile, provide credentials
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=